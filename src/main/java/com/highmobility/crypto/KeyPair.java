package com.highmobility.crypto;

/**
 * will be removed in next future release
 */
@Deprecated
public class KeyPair extends HMKeyPair {

    public KeyPair(byte[] privateKey, byte[] publicKey) {
        super(privateKey, publicKey);
    }
}
